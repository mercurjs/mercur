# Required for Yarn-based installs: Medusa is not compatible with Plug'n'Play, and hosts that
# build the monorepo (e.g. Medusa Cloud) expect node_modules with workspace-scoped hoisting.
nodeLinker: node-modules
nmHoistingLimits: workspaces
