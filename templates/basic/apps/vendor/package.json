{
  "name": "@acme/vendor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 7001",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview --port 7001"
  },
  "dependencies": {
    "@mercurjs/vendor": "2.0.2",
    "@acme/api": "*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
