/**
 * Generated by orval v7.3.0 🍺
 * Do not edit manually.
 * Medusa API
 * OpenAPI spec version: 1.0.0
 */
import type { AdminGetInventoryItemsLocationLevelsLocationId } from './adminGetInventoryItemsLocationLevelsLocationId';
import type { AdminGetInventoryItemsAndItem } from './adminGetInventoryItemsAndItem';
import type { AdminGetInventoryItemsOrItem } from './adminGetInventoryItemsOrItem';

export type AdminGetInventoryItemsParams = {
/**
 * Comma-separated fields that should be included in the returned data. if a field is prefixed with `+` it will be added to the default fields, using `-` will remove it from the default fields. without prefix it will replace the entire default fields.
 */
fields?: string;
/**
 * The number of items to skip when retrieving a list.
 */
offset?: number;
/**
 * Limit the number of items returned in the list.
 */
limit?: number;
/**
 * The field to sort the data by. By default, the sort order is ascending. To change the order to descending, prefix the field name with `-`.
 */
order?: string;
/**
 * The inventory item's q.
 */
q?: string;
id?: string | string[];
sku?: string | string[];
origin_country?: string | string[];
mid_code?: string | string[];
hs_code?: string | string[];
material?: string | string[];
/**
 * The inventory item's requires shipping.
 */
requires_shipping?: boolean;
/**
 * Filter the inventory item's weight.
 */
weight?: {
  /** Filter arrays that contain some of the values of this parameter. */
  $contains?: string[];
  /** Filter by an exact match. */
  $eq?: string;
  /** Filter by values greater than this parameter. Useful for numbers and dates only. */
  $gt?: string;
  /** Filter by values greater than or equal to this parameter. Useful for numbers and dates only. */
  $gte?: string;
  /** Apply a case-insensitive `like` filter. Useful for strings only. */
  $ilike?: string;
  /** Filter by values in this array's items. */
  $in?: string[];
  /** Apply a `like` filter. Useful for strings only. */
  $like?: string;
  /** Filter by values less than this parameter. Useful for numbers and dates only. */
  $lt?: string;
  /** Filter by values less than or equal to this parameter. Useful for numbers and dates only. */
  $lte?: string;
  /** Filter by values not matching this parameter. */
  $ne?: string;
  /** Filter by values not in this array's items. */
  $nin?: string[];
  /** Apply a regex filter. Useful for strings only. */
  $re?: string;
};
/**
 * Filter the inventory item's length.
 */
length?: {
  /** Filter arrays that contain some of the values of this parameter. */
  $contains?: string[];
  /** Filter by an exact match. */
  $eq?: string;
  /** Filter by values greater than this parameter. Useful for numbers and dates only. */
  $gt?: string;
  /** Filter by values greater than or equal to this parameter. Useful for numbers and dates only. */
  $gte?: string;
  /** Apply a case-insensitive `like` filter. Useful for strings only. */
  $ilike?: string;
  /** Filter by values in this array's items. */
  $in?: string[];
  /** Apply a `like` filter. Useful for strings only. */
  $like?: string;
  /** Filter by values less than this parameter. Useful for numbers and dates only. */
  $lt?: string;
  /** Filter by values less than or equal to this parameter. Useful for numbers and dates only. */
  $lte?: string;
  /** Filter by values not matching this parameter. */
  $ne?: string;
  /** Filter by values not in this array's items. */
  $nin?: string[];
  /** Apply a regex filter. Useful for strings only. */
  $re?: string;
};
/**
 * Filter by the inventory item's height.
 */
height?: {
  /** Filter arrays that contain some of the values of this parameter. */
  $contains?: string[];
  /** Filter by an exact match. */
  $eq?: string;
  /** Filter by values greater than this parameter. Useful for numbers and dates only. */
  $gt?: string;
  /** Filter by values greater than or equal to this parameter. Useful for numbers and dates only. */
  $gte?: string;
  /** Apply a case-insensitive `like` filter. Useful for strings only. */
  $ilike?: string;
  /** Filter by values in this array's items. */
  $in?: string[];
  /** Apply a `like` filter. Useful for strings only. */
  $like?: string;
  /** Filter by values less than this parameter. Useful for numbers and dates only. */
  $lt?: string;
  /** Filter by values less than or equal to this parameter. Useful for numbers and dates only. */
  $lte?: string;
  /** Filter by values not matching this parameter. */
  $ne?: string;
  /** Filter by values not in this array's items. */
  $nin?: string[];
  /** Apply a regex filter. Useful for strings only. */
  $re?: string;
};
/**
 * Filter by the inventory item's width.
 */
width?: {
  /** Filter arrays that contain some of the values of this parameter. */
  $contains?: string[];
  /** Filter by an exact match. */
  $eq?: string;
  /** Filter by values greater than this parameter. Useful for numbers and dates only. */
  $gt?: string;
  /** Filter by values greater than or equal to this parameter. Useful for numbers and dates only. */
  $gte?: string;
  /** Apply a case-insensitive `like` filter. Useful for strings only. */
  $ilike?: string;
  /** Filter by values in this array's items. */
  $in?: string[];
  /** Apply a `like` filter. Useful for strings only. */
  $like?: string;
  /** Filter by values less than this parameter. Useful for numbers and dates only. */
  $lt?: string;
  /** Filter by values less than or equal to this parameter. Useful for numbers and dates only. */
  $lte?: string;
  /** Filter by values not matching this parameter. */
  $ne?: string;
  /** Filter by values not in this array's items. */
  $nin?: string[];
  /** Apply a regex filter. Useful for strings only. */
  $re?: string;
};
/**
 * Filter by the inventory item's associated location IDs.
 */
location_levels?: {
  location_id: AdminGetInventoryItemsLocationLevelsLocationId;
};
$and?: AdminGetInventoryItemsAndItem[];
$or?: AdminGetInventoryItemsOrItem[];
};
